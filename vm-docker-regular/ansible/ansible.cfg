# Ansible settings: connect as fabricator using the generated inventory.
[defaults]
inventory             = inventory/inventory.ini
remote_user           = fabricator
host_key_checking     = False
retry_files_enabled   = False
interpreter_python    = auto_silent
roles_path            = roles

[privilege_escalation]
become        = True
become_method = sudo
