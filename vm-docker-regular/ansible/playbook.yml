# Entry point playbook: applies roles to the freshly cloned VM.
---
- name: Configure freshly cloned Proxmox VM
  hosts: pve_service
  become: true

  roles:
    # zram is skipped on Docker hosts per instructions/instruction-01.txt.
    - role: base
      vars:
        base_install_zram: false
    - docker-ce
